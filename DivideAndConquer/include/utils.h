#pragma once
#include <vector>
#include <string>

bool stringParser(std::string &msg, std::vector<int> &result);

int sumElems(std::vector<int> &vec);
void findMinMax(std::vector<int> &vec, int **min_max);
void quickSort(std::vector<int> &vec);